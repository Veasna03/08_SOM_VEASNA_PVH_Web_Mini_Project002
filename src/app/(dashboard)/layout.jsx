import "../globals.css";

export default function DashboardPageLayout({children}){
    return (
        <html lang="en">

            <body>    
                {children}
            </body>
        </html>
    )
}